# 24 설비펌프 v5.0 — GitHub 업로드 지침

작성 2026-09-01 · 저장소 `manminkim-eng/Booster-Pump` · 브랜치 `main`

---

## 1. 업로드 대상 5건

| 파일 | 배포본 | 업로드본 | 구분 |
|---|---|---|---|
| `index.html` | 170,627 B | **171,316 B** | 변경 |
| `sw.js` | 1,986 B | **4,329 B** | 변경 |
| `manifest.json` | 1,581 B | **1,581 B** | ⚠ **크기 동일 · 내용 다름** |
| `assets/fonts/manmin-fonts.css` | 없음 | 1,974 B | 신규 |
| `assets/fonts/NotoSansKR-var.woff2` | 없음 | 427,968 B | 신규 |

### ⚠ `manifest.json` 을 빠뜨리지 말 것

`MANMIN-Ver2.0` → `MANMIN-Ver5.0` 은 글자 수가 같아 **파일 크기가 1,581 B 로 똑같다.**
육안·크기로는 구분이 안 되지만 내용은 다르다 (SHA `6fce59d4…` → `c93da7fa…`).
안 올리면 PWA 설치명이 Ver2.0 으로 남는다. 22번 `sw.js` 와 같은 함정이다.

### git blob SHA-1 (업로드 후 대조용)

```
index.html                        a460e01f6e1dcca34127e1bc5e6d34d70671fdd6
sw.js                             4761d90edb82f7a55f25bfc9ff9532676c23d0ce
manifest.json                     c93da7fa5037163a54dc8f00a52063ea7cbf1678
assets/fonts/manmin-fonts.css     104ae38c871ae9ccb1d7cabe9df794b55b9f8cab
assets/fonts/NotoSansKR-var.woff2 a31539a73890188d4e153826d2ae9ad7c47cb04a
```

---

## 2. 손대지 말 것

| 대상 | 상태 |
|---|---|
| `icons/` 15개 | ✅ 로컬·배포본 SHA **전부 일치** — 다시 올릴 필요 없음 |
| `icons/icon-maskable-512x512.png` | 배포본에만 있음 (236,185 B) · 그대로 둔다 |
| `README.md` | v2.0 기준 · 이번 범위 밖 (관리대장 갱신 때 함께) |
| `offline.html` | 배포본에만 있음 (1,878 B) · **참조처 0건** — 판단 대기 |

---

## 3. 드래그 순서 — 2회로 나눈다

GitHub 웹UI 는 **폴더를 드래그해야 경로가 유지**된다.
폴더 안 파일만 골라 끌면 루트에 평평하게 올라가 `assets/fonts/` 가 깨진다.

### 1회차 — 루트 파일 3개

`_업로드_2026-09-01_Booster-Pump\` 에서 아래 셋만 선택해 드래그

```
index.html
sw.js
manifest.json
```

⛔ `_업로드지침.md` 는 올리지 않는다.

### 2회차 — assets 폴더째

`_업로드_2026-09-01_Booster-Pump\assets` **폴더 자체**를 드래그

```
assets/
  └ fonts/
      ├ manmin-fonts.css
      └ NotoSansKR-var.woff2
```

커밋 메시지 예: `v5.0 디자인 통일 — 3단 헤더·A4 여백 1:1·JPG 블록 쪽나눔·FAB 스택·로컬 폰트`

---

## 4. 업로드 후 검증

주소창에 아래를 직접 타이핑해 실행한다.
(`javascript:` 는 붙여넣기 시 지워지므로 앞부분을 손으로 친다)

```js
javascript:(async()=>{const R='https://manminkim-eng.github.io/Booster-Pump/';
const g=async p=>{const x=await fetch(R+p+'?t='+Date.now(),{cache:'no-store'});return x.ok?await x.text():null;};
const h=await g('index.html'),s=await g('sw.js'),m=await g('manifest.json'),f=await g('assets/fonts/manmin-fonts.css');
console.table({
 '헤더 래퍼':/class="hdr-shell"/.test(h||''),
 '3단 헤더':/hdr-h1/.test(h||''),
 '@page 여백':/margin:14mm 12mm 22mm 14mm/.test(h||''),
 '화면 1:1':/padding:53px 45px 83px 53px/.test(h||''),
 'JPG 블록나눔':/function mmBlocks/.test(h||''),
 '구 절단로직 제거':!/mmPickCutPreferSection/.test(h||''),
 'S24 모달 제거':!/s24-frame|openMP/.test(h||''),
 '탭 순서(기준표→A4)':/tabRef[\s\S]{0,200}tabA4/.test(h||''),
 'FAB 68px':/width:68px;min-width:68px/.test(h||''),
 '인쇄 폰트강제 해제':!/font-size:9\.5px!important/.test(h||''),
 '스케일 해제':/restoreScale/.test(h||''),
 'manifest Ver5.0':/MANMIN-Ver5\.0/.test(m||''),
 '로컬폰트 배포':!!f,
 'SW 캐시명':(s||'').match(/pump-v[\d.]+/)?.[0],
 'SW navigate 분기':/destination === 'document'/.test(s||'')
});})()
```

**기대값** — `SW 캐시명` 이 `"pump-v5.0.2"`, 나머지 전부 `true`

---

## 5. 화면이 안 바뀌면 — 서비스워커 정리

같은 origin 에 39종이 배포돼 SW 가 누적된다(실측 28개 등록).
구버전 SW 가 남아 있으면 서버를 고쳐도 화면이 그대로다.

```js
javascript:(async()=>{for(const r of await navigator.serviceWorker.getRegistrations())await r.unregister();for(const k of await caches.keys())await caches.delete(k);location.reload(true);})()
```

또는 F12 → Application → Storage → Clear site data

---

## 6. 업로드 전 확인 — 실기기 실측이 아직 남아 있다

작업지시서 §2⑤ — 정적 검증만으로는 회귀를 못 잡는다.
`버전s-5.0\_점검_모바일실측_2026-09-01.js` 를 Console 에 붙여 넣어
`1024 · 860 · 768 · 640 · 480 · 420 · 360` 폭에서 통과를 확인한 뒤 올린다.

확인할 것 세 가지
1. JPG 가 **6매**로 나오고 각 매 시작이 PDF 각 쪽과 같은가
2. 폰트 확대 후에도 PDF 가 **6쪽**인가
3. FAB 두 버튼의 폭·높이가 같은가
